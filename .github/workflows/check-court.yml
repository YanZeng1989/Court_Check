name: Check tennis court availability

on:
  # No schedule inside GitHub — triggered externally (e.g. by cron-job.org
  # calling the workflow_dispatch API every 5 minutes). Also runnable
  # manually from the Actions tab any time.
  workflow_dispatch: {}

# Prevents overlapping runs if two triggers land close together. This
# matters more than ever now: two runs racing would both read the same
# next_park_state.txt and could commit conflicting/duplicate rotation
# updates.
concurrency:
  group: check-court-availability
  cancel-in-progress: false

# Needed so the "Persist rotation state" step below can push a commit
# back to this repo (next_park_state.txt).
permissions:
  contents: write

jobs:
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install playwright jpholiday
          playwright install --with-deps chromium

      - name: Write config.txt from secrets
        run: |
          cat > config.txt << EOF
          TELEGRAM_BOT_TOKEN=${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID=${{ secrets.TELEGRAM_CHAT_ID }}
          WATCH=${{ secrets.WATCH }}
          WEEKEND_ALL_DAY=${{ secrets.WEEKEND_ALL_DAY }}
           DEBUG=true
          EOF

      - name: Run checker
        run: python check_court_availability.py

      - name: Persist rotation state
        # Commits next_park_state.txt back to the repo so the NEXT run
        # (which starts from a fresh checkout) knows where this run left
        # off — see NEXT_PARK_STATE_PATH in check_court_availability.py.
        # `if: always()` so this still runs (and the resume point still
        # gets saved) even if the checker step above hit an error.
        if: always()
        run: |
          if [ -f next_park_state.txt ] && ! git diff --quiet -- next_park_state.txt; then
            git config user.name "github-actions[bot]"
            git config user.email "github-actions[bot]@users.noreply.github.com"
            git add next_park_state.txt
            git commit -m "chore: update park check rotation state [skip ci]"
            git push
          else
            echo "No rotation-state change to commit."
          fi

      - name: Upload debug artifacts (only present if something failed)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: debug-output
          path: |
            debug_error.png
            debug_error.html
            debug_calendar_sample.png
            debug_calendar_sample.html
            log.txt
          if-no-files-found: ignore
